import React from 'react';
import { LoginForm } from '@/components/organisms/LoginForm';
import SafeAreaContainer from '@/components/templates/SafeAreaContainer';

const Login = () => {
  return (
    <SafeAreaContainer>
      <LoginForm />
    </SafeAreaContainer>
  );
};

export default Login;
